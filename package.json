{
  "name": "gitvault-live",
  "version": "0.1.0",
  "private": true,
  "description": "Git-based Obsidian sync — desktop plugin (auto-sync, inline collab decorations, squash-to-main) + headless agent git client. Standard hosted git (HTTPS+token) as SoT.",
  "license": "MIT",
  "workspaces": [
    "plugin",
    "daemon"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:plugin": "npm run build -w plugin",
    "build:daemon": "npm run build -w daemon",
    "dev:plugin": "npm run dev -w plugin"
  },
  "engines": {
    "node": ">=18"
  }
}
