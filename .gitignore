node_modules/
dist/
*.log
.DS_Store

# plugin build output (esbuild → plugin/main.js, 번들된 plugin/daemon.js)
main.js
plugin/main.js
daemon.js
plugin/daemon.js
*.js.map

# test bundle output (esbuild) — glob 으로 신규 테스트도 자동 커버(개별 나열 시 누락 drift)
_*.cjs

# secrets / local runtime
.env
.env.*
*.key
id_ed25519
id_ed25519.pub
# 플러그인 런타임 설정(토큰 평문 저장) — 절대 커밋 금지
data.json
plugin/data.json
# daemon env (토큰 포함)
daemon/deploy/daemon.env
# 로컬 vault 설정
.obsidian/
# 로컬 내부 노트(handoff·설계 히스토리) — 공개 안 함
notes/
